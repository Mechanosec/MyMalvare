# Історичне очищення generic-знахідок

За окремим дозволом власника видалено рівно 212 історичних
`generic_high_entropy` знахідок, чиї шляхи є dependency lockfiles і
підпадають під поточний `isExcludedPath`. Перелік ID та зріз до зміни:
[`generic-history-preview-2026-09-22.json`](generic-history-preview-2026-09-22.json).
У preview немає значень, контексту чи шляхів знахідок.

Перед видаленням SQLite backup створено локально в
`credsScrapper/apps/api/prisma/dev.backup-2026-09-22-pre-generic-cleanup.db`
(поза Git, режим доступу `0600`). Перевірено збіг кількості рядків та
`PRAGMA quick_check = ok` у копії. Безпосередньо перед видаленням повторно
перевірено зріз, тип і шлях кожного ID. Видалення виконано однією
транзакцією за точним списком ID.

| Стан | Кількість знахідок | SQLite quick_check |
| --- | ---: | --- |
| Backup до очищення | 3 232 919 | ok |
| Поточна база після очищення | 3 232 707 | ok |

Всі інші знахідки залишено. Для багатьох старих `generic_high_entropy`
поля бази не зберігають структури джерела, потрібної для перевірки
медіаданих, SRI чи іншого шуму. Зміни правил детекції вплинуть на нові
сканування; вони не перекласифікують автоматично старі рядки.
