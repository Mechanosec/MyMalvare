{
  "name": "credsscrapper-monorepo",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "podman compose up -d redis && concurrently -n api,web -c blue,green \"npm run start:dev -w apps/api\" \"npm run dev -w apps/web\"",
    "start": "podman compose up -d redis && concurrently -n api,web -c blue,green \"npm run start -w apps/api\" \"npm run dev -w apps/web\"",
    "stop": "podman compose down -v"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
